var a;
const p = parseInt(prompt('Enter your loan amount : '));
const r = parseInt(prompt('Enter nterest rate per period : '));
const n = parseInt(prompt('Enter total number of periods :  '));
a=p(r (1+r)^n) / ( (1+r)^n -1 );
console.log("Payment amount per period : " + a);
